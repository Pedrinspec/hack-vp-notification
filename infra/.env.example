# ============================================================
# Hack VP Notification Service — Variáveis de Ambiente
# ============================================================
# Copie este arquivo para .env e ajuste os valores conforme
# o ambiente local. O arquivo .env NÃO deve ser versionado.
#
# Uso: docker compose --env-file .env up -d
# ============================================================

# ── Spring ───────────────────────────────────────────────────
SPRING_PROFILES_ACTIVE=docker

# ── Kafka ────────────────────────────────────────────────────
# Endereço do broker Kafka acessível pelo container
KAFKA_BOOTSTRAP_SERVERS=kafka:29092

# Consumer group ID (deve ser único por microserviço)
KAFKA_GROUP_ID=notification-service

# ── Tópicos ──────────────────────────────────────────────────
TOPIC_VIDEO_FAILED=video.failed.v1
TOPIC_VIDEO_FAILED_DLQ=video.failed.v1.dlq

# ── Retry ────────────────────────────────────────────────────
# Número total de tentativas (1 original + N retries)
NOTIF_RETRY_MAX_ATTEMPTS=3

# Intervalo entre retries em milissegundos
NOTIF_RETRY_BACKOFF_MS=1000

# ── Logging ──────────────────────────────────────────────────
LOGGING_LEVEL_COM_FIAP_HACKNOTIFICATION=INFO
LOGGING_LEVEL_ORG_APACHE_KAFKA=WARN
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_KAFKA=INFO

# ── JVM ──────────────────────────────────────────────────────
JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -XX:+UseG1GC -Djava.security.egd=file:/dev/./urandom
